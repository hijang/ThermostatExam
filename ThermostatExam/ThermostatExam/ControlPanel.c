#include "ControlPanel.h"

#include <assert.h>

void  ContolPanel_TurnGreenLED()
{
	// target 환경이 아니면 동작하지 않는다.
	assert(0);	
}

void  ContolPanel_TurnRedLED()
{
	// target 환경이 아니면 동작하지 않는다.
	assert(0);
}

void  ContolPanel_TurnOffLED()
{
	// target 환경이 아니면 동작하지 않는다.
	assert(0);
}